import { CitysearchPipe } from './citysearch.pipe';

describe('CitysearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CitysearchPipe();
    expect(pipe).toBeTruthy();
  });
});
